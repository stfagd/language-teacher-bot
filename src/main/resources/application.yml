# src/main/resources/application.yml

server:
  port: ${PORT:8080}

spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: password

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect

  h2:
    console:
      enabled: true
      path: /h2-console

  sql:
    init:
      mode: never

telegram:
  bot:
    token: 7645031103:AAEQbnbaDHcOAI-xdBZ1PxnrcQV1trPkiu0